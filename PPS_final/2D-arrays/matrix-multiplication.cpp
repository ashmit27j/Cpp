#include<iostream>
using namespace std;
int main()
{
    int m1, n1, m2, n2;
    cout << "Enter row x column for arr1";
    cin >> m1 >> n1;
    cout << "Enter row x column for arr2";
    cin >> m2 >> n2;
    int arr1[m1][n1];
    int arr2[m2][n2];
    cout << "Enter array 1";
    for (int i = 0; i < m1; i ++ )
    {
        for (int j = 0; j < n1; j ++ )
        {
            cin >> arr1[i][j];
        }
    }
    cout << "Enter array 2";
    for (int i = 0; i < m2; i ++ )
    {
        for (int j = 0; j < n2; j ++ )
        {
            cin >> arr2[i][j];
        }
        cout << endl;
    }
    cout << "sum of arrays given: \n";
    if ()
    for (int i = 0; i < m; i ++ )
    {
        for (int j = 0; j < m; j ++ )
        {
            for (int k = 0; k < )
        }
        cout << endl;
    }
    
}